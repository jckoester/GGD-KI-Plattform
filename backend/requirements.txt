# Runtime
fastapi>=0.111.0
weasyprint>=62.0
jinja2>=3.1.0
markdown-it-py>=3.0.0
mdit-py-plugins>=0.4  # dollarmath: $…$/$$…$$ als Math-Token für den PDF-Prärender (Phase 17, D5)
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
python-multipart>=0.0.9
pdfminer.six>=20221105
pgvector>=0.3.0

# Runtime - Server-Rendering: Funktionsgraphen (Phase 17, ```plot)
# matplotlib rendert die Plots in-process zu SVG (kein Sidecar); numpy wertet die
# geparsten Ausdrücke vektorisiert aus; lark parst die Funktions-Terme über eine
# Whitelist-Grammatik (eval-frei — kein sympy/parse_expr).
numpy>=1.26
matplotlib>=3.8
lark>=1.1

# Dev / Test
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-httpx>=0.25.0

# Runtime - Auth
bcrypt>=4.0.0
jsonschema>=4.22.0
python-docx>=1.1

# Runtime - PII-Erkennung (Datensparsamkeit, Phase 14)
# spaCy ist an die Modell-Minor-Version gebunden → 3.8.x. Das deutsche NER-Modell wird
# als gepinntes Wheel installiert (NICHT via `python -m spacy download`, das scheitert in
# uv-/manchen Setups). `pip install -r requirements.txt` holt beides (~45 MB Modell).
spacy>=3.8,<3.9
de_core_news_md @ https://github.com/explosion/spacy-models/releases/download/de_core_news_md-3.8.0/de_core_news_md-3.8.0-py3-none-any.whl
