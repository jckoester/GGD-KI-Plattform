# Direkte Abhängigkeiten (Quelle für den Lockfile).
#
# Obergrenzen (Sicherheits-Audit #17): jede Abhängigkeit hat eine obere Schranke, damit ein
# nächster Major nicht ungetestet eingezogen wird. Für reproduzierbare/produktive Installs den
# gepinnten + gehashten Lockfile nutzen:
#     uv pip install --require-hashes -r requirements.lock
# Nach dem Ändern einer Grenze hier den Lockfile neu erzeugen (siehe unten) und Tests laufen lassen:
#     uv pip compile requirements.txt --generate-hashes -o requirements.lock

# Runtime
fastapi>=0.111.0,<1.0
weasyprint>=62.0,<70.0
jinja2>=3.1.0,<4.0
markdown-it-py>=3.0.0,<5.0
uvicorn[standard]>=0.29.0,<1.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0,<3.0
sqlalchemy[asyncio]>=2.0.30,<3.0
asyncpg>=0.29.0,<1.0
psycopg2-binary>=2.9.0,<3.0
alembic>=1.13.0,<2.0
python-dotenv>=1.0.0,<2.0
pyyaml>=6.0.1,<7.0
python-jose[cryptography]>=3.3.0,<4.0
httpx>=0.27.0,<1.0
python-multipart>=0.0.9,<1.0
# pdfminer.six nutzt Datums-Versionen (YYYYMMDD) — eine semver-Obergrenze ist bedeutungslos;
# die exakte Version pinnt der Lockfile.
pdfminer.six>=20221105
pgvector>=0.3.0,<1.0

# Dev / Test
pytest>=8.0.0,<10.0
pytest-asyncio>=0.23.0,<2.0
pytest-httpx>=0.25.0,<1.0

# Runtime - Auth
bcrypt>=4.0.0,<5.0
jsonschema>=4.22.0,<5.0
python-docx>=1.1,<2.0

# Runtime - PII-Erkennung (Datensparsamkeit, Phase 14)
# spaCy ist an die Modell-Minor-Version gebunden → 3.8.x. Das deutsche NER-Modell wird
# als gepinntes Wheel installiert (NICHT via `python -m spacy download`, das scheitert in
# uv-/manchen Setups). `pip install -r requirements.txt` holt beides (~45 MB Modell).
# Der Integritäts-Hash des Wheels steht im Lockfile (requirements.lock).
spacy>=3.8,<3.9
de_core_news_md @ https://github.com/explosion/spacy-models/releases/download/de_core_news_md-3.8.0/de_core_news_md-3.8.0-py3-none-any.whl
